# ── Large dataset (organiser property — exceeds GitHub's 100 MB limit) ──
*.jsonl
*.jsonl.gz
**/candidates.jsonl
**/candidates.jsonl.gz

# ── macOS / OS junk ──
__MACOSX/
.DS_Store
**/.DS_Store
Thumbs.db

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ── Precomputed artifacts / audit output (regenerated by the code) ──
redrob-ranker-v2/artifacts/*.pkl
redrob-ranker-v2/artifacts/*.npy
redrob-ranker-v2/artifacts/*.bin
redrob-ranker-v2/artifacts/onnx/
redrob-ranker-v2/compliance/audit_trail/*.json

# ── Temp / scratch ──
_*.py
_*.log
_*.err
_test*.jsonl
_smoke_export.xlsx
